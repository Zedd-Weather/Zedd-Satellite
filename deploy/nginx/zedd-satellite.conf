server {
    listen 443 ssl http2;
    server_name satellite.example.com;

    ssl_certificate     /etc/letsencrypt/live/satellite.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/satellite.example.com/privkey.pem;

    auth_basic "Zedd-Satellite";
    auth_basic_user_file /etc/nginx/.htpasswd-zedd-satellite;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options DENY always;
    add_header Referrer-Policy no-referrer always;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
